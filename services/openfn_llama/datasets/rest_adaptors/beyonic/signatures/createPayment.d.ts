/**
 * Create a payment
 * @example
 * execute(
 *   createPayment(data)
 * )(state)
 * @function
 * @param {object} data - Payload data for the payment
 * @returns {Operation}
 */
export function createPayment(data: object): Operation;