/**
 * Create a contact
 * @example
 * execute(
 *   createContact(data)
 * )(state)
 * @function
 * @param {object} data - Payload data for the contact
 * @returns {Operation}
 */
export function createContact(data: object): Operation;